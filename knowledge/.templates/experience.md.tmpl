# 經驗

<!--
  這份文件記錄從開發過程中蒸餾出的教訓——不是 changelog，
  而是應該影響未來決策的模式。

  每個教訓記錄「理論」和「現實」之間的落差。
  保持簡短、可操作。一條教訓背後完整的因果軌跡（怎麼演變、考慮過什麼）放在 knowledge/history/。
-->

## 教訓

<!--
  每個重要的教訓加一個小節。

  一律要有：實際發生 · 教訓 · 來源。

  開頭二選一，看哪個貼：
    - 「理論說」　　— 預期和實際之間有落差
    - 「怎麼撞到的」— 程序型教訓，沒有那個落差（「我這樣做了，而它救了我」）。
    　　　　　　　　 硬套「理論說」會逼出一個編造的錯誤預期，
    　　　　　　　　 那比格式不一致更糟。

  可選，而通常值得那個位置：
    - 「不適用的時候」— 邊界。一條沒有邊界的教訓會被過度套用，
      　結果是每換一個處境就重寫一次，而不是把它磨利一次。
    - 「相關」— 指向同族教訓的橫向連結。
      　一條教訓屬於哪一族，往往比那條教訓本身更有用。

  來源是最先掉的那條腿，尤其在你開始改寫這個格式之後。
  掉了它，結論還在，而產生它的那次場合沒了——你再也重建不回當時的思路。

  教訓要寫成「判準」，不是「做法」。做法（「把綁定層與算繪分離就能測」）
  會隨技術棧過期；判準（「一個會改變被測對象的量測器，量到的不是被測對象」）
  才跨處境。一條教訓完成的標誌，是它改掉了某個判準的寫法，
  不是它被寫下來了。

  別人常記下的教訓（看看有沒有戳中你踩過的）：
    1. 動手前先 print 定位，別急著用推理找 bug
    2. 大重構時，讓編譯器／型別檢查當「待改清單」
    3. 效能多半來自資料模型，不是微優化
    4.「錯 → 懂 → 修」勝過「想到完美再動手」
    5. 別跳過 TDD——「之後再補測試」通常 = debug 三倍久
-->

### [教訓標題]

- **理論說**：[基於設計、原則或假設，你預期會發生什麼]
- **實際發生**：[真正發生了什麼——要具體]
- **解決方式**：[如何彌合落差——修復、替代方案或洞察]
- **教訓**：[一句話的可複用判準]
- **不適用的時候**：[邊界——它在哪裡就不成立了。可選，但這是擋住過度套用的東西]
- **來源**：[history/ 記錄、episode，或 PR/commit 連結]

<!--
  範例：

  ### 實作前先確認功能是否已存在

  - **理論說**：核心模組只處理求值，所以我們需要從頭建一個
    type checker。
  - **實際發生**：花了兩天在建，結果發現核心模組已經有完整的
    型別檢查功能——只是沒有文件記錄。
  - **解決方式**：刪除重複的實作，為既有功能補上文件。
  - **教訓**：動手之前先搜尋既有程式碼。答案可能已經在那裡，
    只是沒被記錄。
  - **來源**：history/010-duplicate-checker.md
-->

## 關鍵延伸（主題觸發必讀）

<!--
  這張表是 /knowie-next 和 /knowie-judge 的「閱讀路由」。
  主文只寫蒸餾後的教訓；完整的因果軌跡放在 history/ 子目錄。
  當某個主題的關鍵字被觸發時，動手前 MUST 先讀對應的 sub-file。
  一開始可以留空；每當你把細節移到子目錄，就回來補一列。
-->

| 觸發關鍵字 | MUST 讀 |
|---|---|
| [關鍵字 / 主題] | `history/[檔名].md` |
